# SM2 + SM4 答辩前 Checklist

这份清单用于正式答辩前 5～10 分钟快速确认。目标不是再次开发功能，而是确保环境、密码链路、中文界面和演示材料都处于已验证状态。

## A. 仓库与环境

- [ ] 当前分支为 `sm2-sm4-backend`
- [ ] `git status --short` 为空
- [ ] 使用 WSL 原生检出作为正式运行目录
- [ ] 激活 `.venv-sm2-sm4`
- [ ] `python tools/patch_gmssl_python_abi.py --check` 显示 `ABI CHECK: PASS`
- [ ] `sizeof(Sm2Key)=128`
- [ ] `sizeof(Sm4Gcm)=296`
- [ ] WSLg 可正常显示窗口
- [ ] 中文字体正常，无 tofu / 方框

## B. 最终自动测试

答辩前至少完整跑一次 v0.5 focused suite。

目标：

```text
43 passed
0 failed
0 skipped
```

如果出现 skip，尤其 real GmSSL integration 被 skip，不视为通过。

## C. 演示文件

提前准备一个体积较小、容易肉眼识别的媒体或二进制文件，例如：

```text
demo.png
sample.jpg
demo.bin
```

建议：

- [ ] 文件体积适中，避免现场等待
- [ ] 文件名简单，不含异常字符
- [ ] 预先记住原文件位置
- [ ] 工作区使用新的 `/tmp/...` 目录，避免旧输出干扰

## D. 中文界面启动

推荐：

```bash
python -m src.sm2_sm4_workbench \
  --workspace /tmp/sm2-sm4-defense
```

确认：

- [ ] 标题为中文
- [ ] 六阶段流程条中文正常
- [ ] 密码框使用掩码
- [ ] 答辩提示条可见
- [ ] 关系图 / 接收端路径 / 原理状态 Tab 均正常

## E. 3～5 分钟现场流程

### 1. 用户与授权集合

- [ ] 为 u1-u4 生成真实 SM2 密钥
- [ ] 选择 `S={u2,u4}`
- [ ] 选择演示媒体

答辩说明：

> u2、u4 是本次授权接收者；u1、u3 不会得到自己的内容密钥封装。

### 2. 生成内容密钥 K

- [ ] 点击“生成内容密钥与会话材料”
- [ ] 记录/指出 K 的 SM3 指纹

答辩说明：

> 一次广播只生成一个随机 128-bit SM4 内容密钥 K，UI 只显示其不可逆指纹，不显示 raw K。

### 3. SM2 密钥封装

在关系图中：

- [ ] `K -> PK[u2]`
- [ ] `K -> PK[u4]`

答辩说明：

> 对每个授权接收者，使用其 SM2 公钥封装同一个 K，得到 `E[u2]` 和 `E[u4]`。

### 4. SM4-GCM 媒体加密

- [ ] `K -> SM4-GCM`
- [ ] 确认 `payload_encryption_count == 1`

答辩说明：

> 媒体主体只加密一次。接收者增多时增长的是 `E[i]` 头部，而不是媒体密文副本。

### 5. 组装广播包

- [ ] 点击“组装 .smre 广播包”
- [ ] 确认广播包节点进入已组装状态

答辩说明：

```text
P = manifest || {E_i}_{i in S} || C_M
```

所有用户接收的是同一份 `.smre`。

### 6. 授权用户 u2

- [ ] 选择 u2
- [ ] 正常解密
- [ ] 状态为 `SUCCESS`
- [ ] 输出文件与原文件逐字节一致
- [ ] 接收端路径全绿
- [ ] 接收端恢复 K 指纹 == 发送端 K 指纹

答辩说明：

> u2 使用 SK[u2] 解开 E[u2]，恢复同一个 K，再通过 SM4-GCM 认证解密得到明文。

### 7. 非接收者 u1

- [ ] 选择 u1
- [ ] 正常解密
- [ ] 状态为 `NOT_RECIPIENT`
- [ ] 不产生明文文件

答辩说明：

> 广播包中根本不存在 `E[u1]`，所以正常接收路径在密钥封装检查处停止。

### 8. 错误私钥强制尝试

- [ ] attacker = u1
- [ ] target = u2
- [ ] 执行 force try
- [ ] 状态为 `SM2_UNWRAP_FAILED`
- [ ] 不产生明文文件

答辩说明：

> 这不是 UI 禁止，而是真实调用 native GmSSL 后，SK[u1] 无法解封 E[u2]，失败发生在 SM2 层。

### 9. 原理解释层

至少点击并解释：

- [ ] SM4 内容密钥 K
- [ ] SM2 密钥封装 E[i]
- [ ] SM4-GCM
- [ ] 广播包
- [ ] SM2 解封

强调：

- [ ] UI 展示输入 / 输出 / 核心关系 / 本次真实状态
- [ ] UI 不展示 raw K
- [ ] UI 不展示 SM2 私钥正文

## F. 答辩时必须使用的准确术语

推荐说法：

> 本项目实现的是基于 SM2 + SM4-GCM 的多接收者混合加密 / 广播式分发方案。

> 媒体主体只加密一次；每个授权接收者增加一份 SM2 内容密钥封装 E[i]。

> 发送端与授权接收端通过 SM3 指纹证明恢复的是同一个 K，而不在 UI 暴露 K 本身。

不要宣称：

- [ ] 常数大小广播头
- [ ] 完整撤销型 Broadcast Encryption
- [ ] Succinct-LWE 安全性
- [ ] 与论文级高级 BE 构造具有相同安全性质

## G. 常见提问速答

**为什么不用 SM2 直接加密媒体？**

SM2 是公钥密码，适合密钥封装；大媒体使用 SM4 对称加密更高效，因此采用混合加密。

**为什么多个用户收到同一个包还能分别解密？**

媒体密文共用同一个 K，但包中为每个授权用户保存了不同的 `E[i]=SM2.Enc(PK_i, K...)`。每个用户只能用自己的私钥恢复 K。

**u1 为什么失败？**

正常路径没有 `E[u1]`；强制拿 SK[u1] 解 E[u2] 时，真实 SM2 解封失败。

**如何证明媒体只加密一次？**

InteractiveSession 维护 `payload_encryption_count`，整个组包前后保持为 1，并有自动测试锁定该不变量。

**为什么 UI 不显示原始 K？**

为了不把秘密材料暴露到可视化层。UI 使用 SM3 fingerprint 证明对象一致性。

## H. 出问题时的最短处理顺序

1. 检查 `git status --short`
2. 检查 venv 是否激活
3. 执行 ABI check
4. 检查 WSLg socket / 重启 WSL
5. 检查 CJK 字体
6. 用 `--lang en_US` 判断是否仅为字体问题
7. 不要临场改密码学 backend

## I. 最终提交前

- [ ] `README.md` 可从项目根目录直接看到运行入口
- [ ] `SM2_SM4_PROJECT_GUIDE.md` 保留完整技术说明
- [ ] 本文件保留答辩前检查流程
- [ ] 最新 focused suite 全绿
- [ ] Git worktree 干净
- [ ] 演示媒体准备完毕
- [ ] 至少完整彩排一次 3～5 分钟流程
